# Simulation dataset

Because the CMAPSS-datasets only contain degradation stage, we have to extend the first cycle up to 50 cycles by copy and add small Gaussian noise. 

Also, doing this could generate the dataset for anomaly detection model